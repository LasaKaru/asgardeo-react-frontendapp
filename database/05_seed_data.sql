-- ============================================
-- Seed Data (Optional)
-- Sample data for testing
-- ============================================

USE `real_estate_db`;

-- Note: In production, users will be created automatically when they login through Asgardeo
-- This is just sample structure, not actual data

-- Display confirmation
SELECT 'Database setup completed successfully!' AS Status;
SELECT 'Tables created: users, auth_tokens, refresh_tokens' AS Info;
SELECT 'Ready for authentication service integration' AS NextStep;
